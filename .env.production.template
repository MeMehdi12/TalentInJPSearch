# =============================================================================
# PRODUCTION ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this to .env in your backend directory on the VPS
# CRITICAL: Never commit the actual .env file to version control!

# --- Environment ---
SEARCH_ENV=production

# --- Qdrant Cloud (Vector Database) ---
QDRANT_URL=https://your-cluster-xxxxx.qdrant.io:6333
QDRANT_API_KEY=your-qdrant-api-key-here

# --- DuckDB Path ---
DUCKDB_PATH=/var/www/talentin/Database/talent_search.duckdb

# --- Embedding Model (MUST match indexed vectors) ---
# Options:
#   - jinaai/jina-embeddings-v2-base-en (768d) - Best for resumes
#   - sentence-transformers/all-mpnet-base-v2 (768d) - General purpose
#   - BAAI/bge-large-en-v1.5 (1024d) - Highest accuracy
EMBEDDING_MODEL=jinaai/jina-embeddings-v2-base-en
EMBEDDING_DIM=768

# --- OpenAI API (For Smart Search) ---
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_MODEL=gpt-4o-mini
OPENAI_MAX_TOKENS=1000
OPENAI_TEMPERATURE=0.1

# --- Feature Flags ---
ENABLE_OPENAI_PARSING=true
ENABLE_TEXT_PARSING=true
ENABLE_FACETED_FILTERS=true

# --- Rate Limiting ---
RATE_LIMIT_RPM=1000
RATE_LIMIT_BURST=100

# =============================================================================
# FRONTEND ENVIRONMENT (.env in frontend directory)
# =============================================================================
# VITE_API_URL=https://yourdomain.com
# VITE_API_KEY=your-secure-api-key-here

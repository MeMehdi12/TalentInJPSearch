# ===================================================================
# Talentin Backend Environment Configuration
# ===================================================================
# 
# INSTRUCTIONS:
# 1. Copy this file to .env in the same directory
# 2. Fill in your actual API keys below
# 3. NEVER commit .env to git (it's in .gitignore)
#
# ===================================================================

# --- ENVIRONMENT ---
# Set to 'production' on server, 'local' for development
SEARCH_ENV=production

# --- OPENAI API ---
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENAI_MODEL=gpt-4o-mini
OPENAI_MAX_TOKENS=1000

# --- QDRANT VECTOR DATABASE ---
# Get your Qdrant Cloud credentials from: https://cloud.qdrant.io
QDRANT_URL=https://xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx.us-east4-0.gcp.cloud.qdrant.io:6333
QDRANT_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Collection names (usually don't need to change these)
QDRANT_COLLECTION=profiles_hybrid
QDRANT_COLLECTION_DENSE=talent_profiles

# --- DUCKDB DATABASE ---
# Path to your DuckDB database file on the server
DATABASE_PATH=/var/www/talentin/database/talent_search.duckdb

# --- SEARCH CONFIGURATION ---
# Maximum number of results to return
DEFAULT_LIMIT=50
MAX_LIMIT=100

# --- LOGGING ---
LOG_LEVEL=INFO

# ===================================================================
# REQUIRED API KEYS SUMMARY:
# ===================================================================
# 
# 1. OPENAI_API_KEY (Required)
#    - Used for: Intelligent query parsing and JD analysis
#    - Get it: https://platform.openai.com/api-keys
#    - Costs: ~$0.0001 per search query (very cheap)
# 
# 2. QDRANT_URL + QDRANT_API_KEY (Required)
#    - Used for: Fast semantic vector search
#    - Get it: https://cloud.qdrant.io (free tier available)
#    - Setup: Create a cluster and collection
# 
# 3. DATABASE_PATH (Required)
#    - Local path to your DuckDB file with candidate data
#    - Make sure the file exists at this path on the server
# 
# ===================================================================
